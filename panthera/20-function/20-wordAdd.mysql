CREATE OR REPLACE FUNCTION `wordAdd`(
  ILANG INT UNSIGNED
) RETURNS INT MODIFIES SQL DATA
BEGIN
    DECLARE word_id INT UNSIGNED; 
    INSERT INTO `words`() VALUES ();
    SELECT LAST_INSERT_ID() INTO word_id;
    SELECT `langAddToWord`(ILANG, word_id);
    RETURN word_id;
END;
